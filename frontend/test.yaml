version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - env | grep -e NEXTAUTH_URL=$NEXTAUTH_URL >> .env.production
            - env | grep -e NEXTAUTH_SECRET=$NEXTAUTH_SECRET >> .env.production
            - env | grep -e NEXT_PUBLIC_BACKEND_URL=$NEXT_PUBLIC_BACKEND_URL >> .env.production
            - env | grep -e GOOGLE_CLIENT_ID=$GOOGLE_CLIENT_ID >> .env.production
            - env | grep -e GOOGLE_CLIENT_SECRET=$GOOGLE_CLIENT_SECRET >> .env.production
            - env | grep -e NEXT_AUTH_AWS_ACCESS_KEY=$NEXT_AUTH_AWS_ACCESS_KEY >> .env.production
            - env | grep -e NEXT_AUTH_AWS_SECRET_KEY=$NEXT_AUTH_AWS_SECRET_KEY >> .env.production
            - env | grep -e NEXT_AUTH_AWS_REGION=$NEXT_AUTH_AWS_REGION >> .env.production
            - env | grep -e TWITTER_CLIENT_ID=$TWITTER_CLIENT_ID >> .env.production
            - env | grep -e TWITTER_CLIENT_SECRET=$TWITTER_CLIENT_SECRET >> .env.production
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
    appRoot: frontend


version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - env | grep -e NEXTAUTH_URL >> .env.production
            - env | grep -e NEXTAUTH_SECRET >> .env.production
            - env | grep -e NEXT_PUBLIC_BACKEND_URL >> .env.production
            - env | grep -e GOOGLE_CLIENT_ID >> .env.production
            - env | grep -e GOOGLE_CLIENT_SECRET >> .env.production
            - env | grep -e NEXT_AUTH_AWS_ACCESS_KEY >> .env.production
            - env | grep -e NEXT_AUTH_AWS_SECRET_KEY >> .env.production
            - env | grep -e NEXT_AUTH_AWS_REGION >> .env.production
            - env | grep -e TWITTER_CLIENT_ID >> .env.production
            - env | grep -e TWITTER_CLIENT_SECRET >> .env.production
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
    appRoot: frontend
